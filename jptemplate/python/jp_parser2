#
# -${short flag}, --${long flag}
#
parser.add_option( '-${short flag}', '--${long flag}',
	dest    = "${long flag}_dest",
	default = "default.out",
	action  = "store_true",
	help    = "${Description}"
)

${cursor}
