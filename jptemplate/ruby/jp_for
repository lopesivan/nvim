${listi name:mylist} = ${input list:[0, 1, 2, 3, 4, 5, 6, 8]}

for ${variable_name:l} in ${listi name:mylist} do
	# execute one for each item in the list until the list is not finished
	# (And repeat all statement between do and done)
	#
	puts ${variable_name:l}
	${cursor}
end
